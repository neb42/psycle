const movingAverageText = (context: any) => {
  const title = 'Moving Average';
  const content = 'Moving average content';

  return {
    title,
    content,
  };
};

export default movingAverageText;