const instructorBarsText = (context: any) => {
  const title = 'Intructor bars';
  const content = 'Instructor bars content';

  return {
    title,
    content,
  };
};

export default instructorBarsText;
